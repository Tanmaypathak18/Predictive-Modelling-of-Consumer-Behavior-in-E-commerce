[build]
builder = "DOCKERFILE"
watchPatterns = ["project_api/**"]

[deploy]
startCommand = "uvicorn api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "on_failure"

[service]
rootDirectory = "project_api"
